classes:
  - keystone
  - keystone::roles::admin
  - keystone::config
  # the endpoint additions and database additions
  # are a little difficult for me
  # I am not sure how it fits into the ideal model
  - openstack_extras::keystone_endpoints
